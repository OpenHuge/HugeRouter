{
  "name": "@huge-router/ts-api-client",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "lint": "eslint . --config ../../eslint.config.mjs",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "test": "node --experimental-strip-types --experimental-specifier-resolution=node --test src/index.test.ts",
    "generate": "node -e \"console.log('Use pnpm generate from the workspace root to refresh contract artifacts')\""
  },
  "dependencies": {
    "@huge-router/ts-shared-schema": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "25.6.0",
    "typescript": "6.0.3"
  }
}
